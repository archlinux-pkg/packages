#!/bin/bash

DIR=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )

source ${DIR}/PKGBUILD

_pkgver() {
  curl --location --silent "https://unpkg.com/${_npm}/package.json" | jq -r ".version"
}

_ver=$(_pkgver)
pkgver=${_ver}
