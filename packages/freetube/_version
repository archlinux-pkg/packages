#!/bin/bash

_repo='FreeTubeApp/FreeTube'
_pkgver() {
  curl --location --silent -H "Authorization: token ${GITHUB_API_TOKEN}" "https://api.github.com/repos/$_repo/tags" | jq -r '.[0].name'
}

_ver=$(_pkgver)
_ver1=${_ver//-/_}
pkgver=${_ver1#[v,r]}
